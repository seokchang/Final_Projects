<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="camp">

  <select id="selectAllCampList" parameterType="map" resultType="clpd">
  		select * from (select rownum as rnum, c.*from(
  			select 
  				camp_no as campNo,
  				ceo_id as ceoId,
  				camp_addr as campAddr,
  				camp_name as campName,
  				camp_site as campSite,
  				camp_tel as campTel,
  				camp_theme as campTheme,
  				
  				
  			from camp order by 1 desc)c) 
  		where rnum between #{start} and #{end}
  </select>
  
  <select id="totalCount" resultType="_int">
  		select count(*) from camp
  </select>
  
  <select id="selectOneCamp" parameterType="string" resultType="c">
  	select
  	
  </select>
  
</mapper>
